# 2026-05-22 — Daily Log

## Session Overview

**Justin woke Holly up at ~08:00 UTC.** Session focused on: QB session keepalive failure, bank balance check, SM8 OAuth expiry, and portal SC form bugs.

## QB Session Issue (08:00-09:30)

**Problem:** QB session expired ~07:06. Keepalive cron kept detecting "session expired" but couldn't re-login. Morning bank balance check reported £0.00 (script bug — wrong URL filter).

**Root cause of keepalive failure:** CDP JavaScript `.click()` on Intuit's React sign-in page doesn't trigger React synthetic events. The account card click appeared to work (URL changed) but no password field appeared.

**Key discovery:** The login is SIMPLE — one or two steps. Password is ALREADY FILLED IN when you click the account card. Just click Continue → "This was me" if needed → Banking.

**Fix applied:**
- `check_bank_balance.py` — new direct CDP script (works correctly)
- `chrome-keepalive.py` — updated with Playwright re-login
- `qb_relogin.py` — new Playwright re-login script

**Bank balances confirmed:** £108,014.12 total
- Revolut: £604.07
- Metro Current: £33,769.05
- Metro Savings: £74,641.00

## SM8 OAuth Issue

ServiceM8 OAuth credentials expired May 11. Refresh fails with "invalid_client". Justin frustrated — weekly expiry cycle. Actual credentials at `~/.openclaw/workspace/.credentials/servicem8_oauth.json`. Vault has only placeholders `[SM8_OAUTH_TOKEN]`.

## Portal SC Form Bugs (from previous session)

1. Step 3 invoice figures wrong (not mirroring SM8 API)
2. Step 1 missing Service Contract V7 checklist item
3. Step 2 not filling SC V7 form / generating smart attachment

## QB Login Protocol (Critical Fix)

**CDP JS `.click()` does NOT work on Intuit's React SPA.** Use Playwright Python with `.locator().click()` instead.

See: `/root/OpenClaw-Wiki/05-Learnings/quickbooks-automation-may-22-2026.md`
